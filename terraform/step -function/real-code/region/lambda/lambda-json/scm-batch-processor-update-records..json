 
{
    "cloudwatch_logs_retention_in_days": 90,
    "create_role": false,
    "description": "Lambda that part of the scm batch step function, this file we update the records and then write a temp file to S3.",
    "function_name": "${PREFIX}-scm-batch-processor-update-records",
    "handler": "scm-batch-processor-update-records.lambda_handler",
    "lambda_role": "${SCM_BATCH_PROCESSOR_UPDATE_RECORDS}",
    "memory_size": "10240",
    "publish": true,
    "runtime": "python3.13",
    "source_path": [
        {
            "path": "${LAMBDA_PATH}/code/scm-batch-processor-update-records.py",
            "pip_requirements": false
        }
    ],
    "timeout": 900,
    "tags": ${TAGS}
}
 